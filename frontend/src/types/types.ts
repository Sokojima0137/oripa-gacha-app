export type Pack = {
    id: number;
    name: string;
    imageUrl: string;
    description: string;
    createdAt: string;
};

export type Item = {
    id: number;
    name: string;
    imageUrl: string;
    description: string;
};